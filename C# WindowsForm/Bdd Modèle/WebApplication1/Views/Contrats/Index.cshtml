@model IEnumerable<WebApplication1.Models.Contrat>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TypeDeContrat)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TypeDeContrat)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdContrat }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdContrat }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdContrat })
        </td>
    </tr>
}

</table>
