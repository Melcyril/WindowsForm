@model IEnumerable<WebApplication1.Models.Notification>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.MessageNotification)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DateNotification)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.MessageNotification)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DateNotification)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdNotification }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdNotification }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdNotification })
        </td>
    </tr>
}

</table>
