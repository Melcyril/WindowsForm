@model IEnumerable<WebApplication1.Models.Competence>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ListeCompetence)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ListeCompetence)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdCompetence }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdCompetence }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdCompetence })
        </td>
    </tr>
}

</table>
