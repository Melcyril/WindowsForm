@model IEnumerable<WebApplication1.Models.Remuneration>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SalaireAnnuelle)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SalaireAnnuelle)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdRemuneration }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdRemuneration }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdRemuneration })
        </td>
    </tr>
}

</table>
