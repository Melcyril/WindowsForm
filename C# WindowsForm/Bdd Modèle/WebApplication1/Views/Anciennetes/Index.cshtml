@model IEnumerable<WebApplication1.Models.Anciennete>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DureeExperience)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DureeExperience)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdAnciennete }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdAnciennete }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdAnciennete })
        </td>
    </tr>
}

</table>
